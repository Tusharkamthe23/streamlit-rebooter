name: Keep Streamlit Apps Awake

on:
  schedule:
    - cron: "0 */8 * * *"   # every 8 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Video Translator
        run: curl -L https://video-player-1.streamlit.app/

      - name: Ping Another App
        run: curl -L https://multimodel-ai-agent.streamlit.app/

      
